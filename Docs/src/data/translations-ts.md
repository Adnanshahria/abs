# File: src/data/translations.ts

## Analysis
- **Type**: Data Module
- **Purpose**: Stores i18n strings for English and Bangla.
- **Key Logic**:
    - Structured object `translations` with keys `en` and `bn`.
    - Used by consumers of `LanguageContext` (though `LanguageContext` logic needs fix).
- **Dependencies**: None.

## Identified Bugs / Issues
- No bugs in the file itself.
- **[Completeness]**: Ensure all UI text is covered here.
